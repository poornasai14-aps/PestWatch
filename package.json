{
  "name": "pestwatch-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "PestWatch — Crop Pest Early-Warning (Capacitor Android wrapper)",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
